# GUI 框架
PySide6>=6.6.0

# HTTP 客户端（Clash API 通信、订阅获取）
httpx>=0.25.0

# 加密
cryptography>=41.0.0

# 系统监控（端口冲突检测、进程管理）
psutil>=5.9.0

# Windows TUN 辅助进程通信（仅 Windows 需要）
pywin32>=306; sys_platform == "win32"
